// Make sure we wait to attach our handlers until the DOM is fully loaded.
$(function() {

  // jQuery event handlers should go here.
// See day planner index.handlebars - send post requests as $.ajax calls type "POST"
// $.ajax("/*whatever we call the route goes here/vice versa*")
});
